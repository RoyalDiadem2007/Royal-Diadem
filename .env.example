# Copy to .env.local (gitignored) and fill in. ONLY public values may live in
# VITE_* variables — they ship to the browser (CLAUDE.md §4.5). Secret keys
# (sb_secret_, Turnstile secret, Anthropic) NEVER go here — they belong in
# `supabase secrets set` / Vercel server env (docs/KEYS_SETUP.md).

VITE_SUPABASE_URL=https://luvthaezikvssnuegviu.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_REPLACE_ME

# Cloudflare's official always-pass TEST site key (public). Replace with the
# real site key from docs/KEYS_SETUP.md §2 before launch.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
